// Used by `next-intl/plugin` during Next.js compilation.
// Keep in sync with `apps/frontend/i18n/routing.ts`.
const config = {
  locales: ['en', 'vi'],
  defaultLocale: 'en',
  localePrefix: 'always'
};

export default config;

